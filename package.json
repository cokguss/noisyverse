{
  "name": "noisy-verse",
  "version": "1.0.0",
  "private": true,
  "description": "Noisy Verse — website/repo to prompt + PRD generator",
  "scripts": {
    "start": "cd backend && node server.js",
    "dev": "cd backend && node --watch server.js",
    "setup": "cd backend && npm install",
    "set-webhook": "node backend/set-webhook.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "@supabase/supabase-js": "^2.45.4"
  },
  "engines": {
    "node": ">=18"
  }
}
